Ths repository demonstrates how to create your own AWS Lambda layer with various tools such as awscli, kubectl and helm.

## Usage

```
# generate the layer.zip from Dockerfile
bash build.sh
```

Deploy the lambda layer to AWS.
